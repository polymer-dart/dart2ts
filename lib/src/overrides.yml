overrides:
 "dart:core":
    classes:
      List:
         to:
          class: DartList
          from: dart_sdk/collection
         properties:
           first: "$first"
           last: "$last"
         methods:
           map: "$map"
      Iterable:
         to:
          class: DartIterable
          from: dart_sdk/collection
         methods:
          where: "$where"
          join: "$join"
